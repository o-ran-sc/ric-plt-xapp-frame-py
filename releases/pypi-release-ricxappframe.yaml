---
pypi_project: ricxappframe
# python version must be parsed as a string, not number
python_version: '3.7'
# version to release
version: 2.2.0
# partial URL of logs from job that staged the package to test.pypi.org
log_dir: ric-plt-xapp-frame-py-pypi-stage-master/11
